{
  "name": "taxpilot-ai",
  "private": true,
  "version": "0.1.0",
  "description": "TaxPilot AI Phase 1 monorepo foundation for a German freelancer tax workflow SaaS MVP.",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @taxpilot/web",
    "dev:api": "npm run dev --workspace @taxpilot/api",
    "build": "npm run build --workspace @taxpilot/shared && npm run build --workspace @taxpilot/api && npm run build --workspace @taxpilot/web",
    "build:web": "npm run build --workspace @taxpilot/web",
    "build:api": "npm run build --workspace @taxpilot/api",
    "build:shared": "npm run build --workspace @taxpilot/shared",
    "prisma:validate": "npm run prisma:validate --workspace @taxpilot/db"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
