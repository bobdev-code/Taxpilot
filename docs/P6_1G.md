# P6.1g

Done:

- added `AccountantPackageSummary` to the web app;
- integrated it into the accountant export preview pane;
- the summary derives package status from the deterministic workspace evaluation;
- the summary shows ready count, open questions, review items, applied rule IDs, source IDs and next actions.

Product result:

- the export area now has a human-readable review summary before the raw JSON;
- users can understand whether the package is blocked, needs review or is ready for review;
- the package remains clearly preliminary and not a filing decision.
