# P6.1f

Done:

- exported the category rule metadata helper from `@taxpilot/rules`;
- added `ReceiptRuleExplanation` to the web app;
- added a per-receipt explanation panel in the selected receipt view;
- included rule metadata in the browser export preview.

Product result:

- users can see why a receipt is flagged;
- selected receipts show rule ids, source ids, review level and required evidence;
- accountant export preview now includes local rule metadata per receipt.

Boundary:

- the explanation is workflow metadata only;
- it does not produce final tax advice or filing conclusions.
