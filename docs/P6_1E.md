# P6.1e

Done:

- added a Tax Rule Registry panel to the web app;
- connected the panel to `GET /api/tax-rules`;
- updated the app header to Phase 6.1;
- made the static registry visible in the product UI.

Product result:

- users can see how many sources, rules and mappings exist;
- selected rule and source metadata is visible without Supabase;
- the future Supabase rule registry remains behind the same API boundary.

Next:

- verify build/deployment;
- later replace static registry source with Supabase-backed repository once the TaxPilot Supabase project is connected.
