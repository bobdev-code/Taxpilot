# P6.1d

Done:

- workspace insights now receive category metadata;
- the rule cockpit displays rule ids, source ids and review level;
- backend export includes workspace rule evaluation.

Notes:

- core receipt evaluation remains deterministic;
- metadata is used for traceability and review context;
- no final tax decision is produced.
